import { CpfCnpjMaskDirective } from './cpf-cnpj-mask.directive';

describe('CpfCnpjMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new CpfCnpjMaskDirective();
    expect(directive).toBeTruthy();
  });
});
